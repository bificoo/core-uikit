import { __exports as prefix$1 } from '../../../../../_virtual/prefix.js';
export { __exports as default } from '../../../../../_virtual/prefix.js';
import '../../../../@babel/runtime/helpers/interopRequireDefault.js';
import curry_1 from '../../../../lodash/curry.js';
import cx from 'classnames';
import { i as interopRequireDefault } from '../../../../../_virtual/interopRequireDefault.js';

var _interopRequireDefault = interopRequireDefault.exports;

prefix$1.__esModule = true;
prefix$1.prefix = prefix$1.defaultClassPrefix = prefix$1.getClassNamePrefix = prefix$1.globalKey = void 0;

var _curry = _interopRequireDefault(curry_1);

var _classnames = _interopRequireDefault(cx);

var globalKey = 'rs-';
prefix$1.globalKey = globalKey;

var getClassNamePrefix = function getClassNamePrefix() {
  return globalKey;
};

prefix$1.getClassNamePrefix = getClassNamePrefix;

var defaultClassPrefix = function defaultClassPrefix(name) {
  return name ? "" + getClassNamePrefix() + name : undefined;
};

prefix$1.defaultClassPrefix = defaultClassPrefix;
var prefix = (0, _curry["default"])(function (pre, className) {
  if (!pre || !className) {
    return '';
  }

  if (Array.isArray(className)) {
    return (0, _classnames["default"])(className.filter(function (name) {
      return !!name;
    }).map(function (name) {
      return pre + "-" + name;
    }));
  }

  return pre + "-" + className;
});
prefix$1.prefix = prefix;
