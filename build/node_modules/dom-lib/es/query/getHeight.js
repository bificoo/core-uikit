'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getWindow = require('./getWindow.js');
var getOffset = require('./getOffset.js');

var getHeight = (function (node, client) {
  var win = getWindow['default'](node);

  if (win) {
    return win.innerHeight;
  }

  return client ? node.clientHeight : getOffset['default'](node).height;
});

exports['default'] = getHeight;
