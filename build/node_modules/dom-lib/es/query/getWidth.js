'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var getWindow = require('./getWindow.js');
var getOffset = require('./getOffset.js');

var getWidth = (function (node, client) {
  var win = getWindow['default'](node);

  if (win) {
    return win.innerWidth;
  }

  if (client) {
    return node.clientWidth;
  }

  var offset = getOffset['default'](node);
  return offset ? offset.width : 0;
});

exports['default'] = getWidth;
