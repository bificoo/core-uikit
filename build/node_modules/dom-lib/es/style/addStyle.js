'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var hyphenateStyleName = require('./hyphenateStyleName.js');
var removeStyle = require('./removeStyle.js');

var addStyle = (function (node, property, value) {
  var css = '';
  var props = property;

  if (typeof property === 'string') {
    if (value === undefined) {
      throw new Error('value is undefined');
    }

    (props = {})[property] = value;
  }

  if (typeof props === 'object') {
    for (var key in props) {
      if (Object.prototype.hasOwnProperty.call(props, key)) {
        if (!props[key] && props[key] !== 0) {
          removeStyle['default'](node, hyphenateStyleName['default'](key));
        } else {
          css += hyphenateStyleName['default'](key) + ":" + props[key] + ";";
        }
      }
    }
  }

  node.style.cssText += ";" + css;
});

exports['default'] = addStyle;
