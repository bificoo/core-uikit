'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var camelizeStyleName = require('./camelizeStyleName.js');
var getComputedStyle = require('./getComputedStyle.js');
var hyphenateStyleName = require('./hyphenateStyleName.js');

var getStyle = (function (node, property) {
  if (property) {
    var value = node.style[camelizeStyleName['default'](property)];

    if (value) {
      return value;
    }

    var styles = getComputedStyle['default'](node);

    if (styles) {
      return styles.getPropertyValue(hyphenateStyleName['default'](property));
    }
  }

  return node.style || getComputedStyle['default'](node);
});

exports['default'] = getStyle;
