import { hyphenate } from './stringFormatter.js';

var msPattern = /^ms-/;
var hyphenateStyleName = (function (string) {
  return hyphenate(string).replace(msPattern, '-ms-');
});

export { hyphenateStyleName as default };
