'use strict';

var getVendorPrefixedName = require('../../../_virtual/getVendorPrefixedName.js_commonjs-exports');
require('./query/canUseDOM.js');
require('./utils/stringFormatter.js');
var canUseDOM = require('../../../_virtual/canUseDOM.js_commonjs-exports');
var stringFormatter = require('../../../_virtual/stringFormatter.js_commonjs-exports');

(function (exports) {

exports.__esModule = true;
exports["default"] = void 0;

var _canUseDOM = _interopRequireDefault(canUseDOM.__exports);

var _stringFormatter = stringFormatter.__exports;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var memoized = {};
var prefixes = ['Webkit', 'ms', 'Moz', 'O'];
var prefixRegex = new RegExp("^(" + prefixes.join('|') + ")");
var testStyle = _canUseDOM["default"] ? document.createElement('div').style : {};

function getWithPrefix(name) {
  for (var i = 0; i < prefixes.length; i += 1) {
    var prefixedName = prefixes[i] + name;

    if (prefixedName in testStyle) {
      return prefixedName;
    }
  }

  return null;
}
/**
 * @param {string} property Name of a css property to check for.
 * @return {?string} property name supported in the browser, or null if not
 * supported.
 */


function getVendorPrefixedName(property) {
  var name = (0, _stringFormatter.camelize)(property);

  if (memoized[name] === undefined) {
    var capitalizedName = name.charAt(0).toUpperCase() + name.slice(1);

    if (prefixRegex.test(capitalizedName)) {
      throw new Error("getVendorPrefixedName must only be called with unprefixed\n          CSS property names. It was called with " + property);
    }

    memoized[name] = name in testStyle ? name : getWithPrefix(capitalizedName);
  }

  return memoized[name];
}

var _default = getVendorPrefixedName;
exports["default"] = _default;
}(getVendorPrefixedName.__exports));
