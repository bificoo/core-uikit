'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _assignMergeValue = require('./_assignMergeValue.js');
require('./_cloneBuffer.js');
var _cloneTypedArray = require('./_cloneTypedArray.js');
var _copyArray = require('./_copyArray.js');
var _initCloneObject = require('./_initCloneObject.js');
var isArguments$1 = require('./isArguments.js');
var isArray$1 = require('./isArray.js');
var isArrayLikeObject$1 = require('./isArrayLikeObject.js');
require('./isBuffer.js');
var isFunction$1 = require('./isFunction.js');
var isObject$1 = require('./isObject.js');
var isPlainObject$1 = require('./isPlainObject.js');
var isTypedArray$1 = require('./isTypedArray.js');
var _safeGet = require('./_safeGet.js');
var toPlainObject$1 = require('./toPlainObject.js');
var _cloneBuffer = require('../../_virtual/_cloneBuffer.js_commonjs-module');
var isBuffer$1 = require('../../_virtual/isBuffer.js_commonjs-module');

var assignMergeValue = _assignMergeValue['default'],
    cloneBuffer = _cloneBuffer._cloneBuffer.exports,
    cloneTypedArray = _cloneTypedArray['default'],
    copyArray = _copyArray['default'],
    initCloneObject = _initCloneObject['default'],
    isArguments = isArguments$1['default'],
    isArray = isArray$1['default'],
    isArrayLikeObject = isArrayLikeObject$1['default'],
    isBuffer = isBuffer$1.isBuffer.exports,
    isFunction = isFunction$1['default'],
    isObject = isObject$1['default'],
    isPlainObject = isPlainObject$1['default'],
    isTypedArray = isTypedArray$1['default'],
    safeGet = _safeGet['default'],
    toPlainObject = toPlainObject$1['default'];

/**
 * A specialized version of `baseMerge` for arrays and objects which performs
 * deep merges and tracks traversed objects enabling objects with circular
 * references to be merged.
 *
 * @private
 * @param {Object} object The destination object.
 * @param {Object} source The source object.
 * @param {string} key The key of the value to merge.
 * @param {number} srcIndex The index of `source`.
 * @param {Function} mergeFunc The function to merge values.
 * @param {Function} [customizer] The function to customize assigned values.
 * @param {Object} [stack] Tracks traversed source values and their merged
 *  counterparts.
 */
function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
  var objValue = safeGet(object, key),
      srcValue = safeGet(source, key),
      stacked = stack.get(srcValue);

  if (stacked) {
    assignMergeValue(object, key, stacked);
    return;
  }
  var newValue = customizer
    ? customizer(objValue, srcValue, (key + ''), object, source, stack)
    : undefined;

  var isCommon = newValue === undefined;

  if (isCommon) {
    var isArr = isArray(srcValue),
        isBuff = !isArr && isBuffer(srcValue),
        isTyped = !isArr && !isBuff && isTypedArray(srcValue);

    newValue = srcValue;
    if (isArr || isBuff || isTyped) {
      if (isArray(objValue)) {
        newValue = objValue;
      }
      else if (isArrayLikeObject(objValue)) {
        newValue = copyArray(objValue);
      }
      else if (isBuff) {
        isCommon = false;
        newValue = cloneBuffer(srcValue, true);
      }
      else if (isTyped) {
        isCommon = false;
        newValue = cloneTypedArray(srcValue, true);
      }
      else {
        newValue = [];
      }
    }
    else if (isPlainObject(srcValue) || isArguments(srcValue)) {
      newValue = objValue;
      if (isArguments(objValue)) {
        newValue = toPlainObject(objValue);
      }
      else if (!isObject(objValue) || isFunction(objValue)) {
        newValue = initCloneObject(srcValue);
      }
    }
    else {
      isCommon = false;
    }
  }
  if (isCommon) {
    // Recursively merge objects and arrays (susceptible to call stack limits).
    stack.set(srcValue, newValue);
    mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
    stack['delete'](srcValue);
  }
  assignMergeValue(object, key, newValue);
}

var _baseMergeDeep = baseMergeDeep;

exports['default'] = _baseMergeDeep;
