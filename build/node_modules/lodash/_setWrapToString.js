'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _getWrapDetails = require('./_getWrapDetails.js');
var _insertWrapDetails = require('./_insertWrapDetails.js');
var _setToString = require('./_setToString.js');
var _updateWrapDetails = require('./_updateWrapDetails.js');

var getWrapDetails = _getWrapDetails['default'],
    insertWrapDetails = _insertWrapDetails['default'],
    setToString = _setToString['default'],
    updateWrapDetails = _updateWrapDetails['default'];

/**
 * Sets the `toString` method of `wrapper` to mimic the source of `reference`
 * with wrapper details in a comment at the top of the source body.
 *
 * @private
 * @param {Function} wrapper The function to modify.
 * @param {Function} reference The reference function.
 * @param {number} bitmask The bitmask flags. See `createWrap` for more details.
 * @returns {Function} Returns `wrapper`.
 */
function setWrapToString(wrapper, reference, bitmask) {
  var source = (reference + '');
  return setToString(wrapper, insertWrapDetails(source, updateWrapDetails(getWrapDetails(source), bitmask)));
}

var _setWrapToString = setWrapToString;

exports['default'] = _setWrapToString;
