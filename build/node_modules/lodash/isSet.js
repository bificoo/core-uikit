'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _baseIsSet = require('./_baseIsSet.js');
var _baseUnary = require('./_baseUnary.js');
require('./_nodeUtil.js');
var _nodeUtil = require('../../_virtual/_nodeUtil.js_commonjs-module');

var baseIsSet = _baseIsSet['default'],
    baseUnary = _baseUnary['default'],
    nodeUtil = _nodeUtil._nodeUtil.exports;

/* Node.js helper references. */
var nodeIsSet = nodeUtil && nodeUtil.isSet;

/**
 * Checks if `value` is classified as a `Set` object.
 *
 * @static
 * @memberOf _
 * @since 4.3.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a set, else `false`.
 * @example
 *
 * _.isSet(new Set);
 * // => true
 *
 * _.isSet(new WeakSet);
 * // => false
 */
var isSet = nodeIsSet ? baseUnary(nodeIsSet) : baseIsSet;

var isSet_1 = isSet;

exports['default'] = isSet_1;
