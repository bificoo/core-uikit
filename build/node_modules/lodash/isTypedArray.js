'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _baseIsTypedArray = require('./_baseIsTypedArray.js');
var _baseUnary = require('./_baseUnary.js');
require('./_nodeUtil.js');
var _nodeUtil = require('../../_virtual/_nodeUtil.js_commonjs-module');

var baseIsTypedArray = _baseIsTypedArray['default'],
    baseUnary = _baseUnary['default'],
    nodeUtil = _nodeUtil._nodeUtil.exports;

/* Node.js helper references. */
var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;

/**
 * Checks if `value` is classified as a typed array.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.
 * @example
 *
 * _.isTypedArray(new Uint8Array);
 * // => true
 *
 * _.isTypedArray([]);
 * // => false
 */
var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;

var isTypedArray_1 = isTypedArray;

exports['default'] = isTypedArray_1;
