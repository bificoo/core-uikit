'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _arrayLikeKeys = require('./_arrayLikeKeys.js');
var _baseKeysIn = require('./_baseKeysIn.js');
var isArrayLike$1 = require('./isArrayLike.js');

var arrayLikeKeys = _arrayLikeKeys['default'],
    baseKeysIn = _baseKeysIn['default'],
    isArrayLike = isArrayLike$1['default'];

/**
 * Creates an array of the own and inherited enumerable property names of `object`.
 *
 * **Note:** Non-object values are coerced to objects.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Object
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.keysIn(new Foo);
 * // => ['a', 'b', 'c'] (iteration order is not guaranteed)
 */
function keysIn(object) {
  return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
}

var keysIn_1 = keysIn;

exports['default'] = keysIn_1;
