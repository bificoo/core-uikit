import React__default from 'react';
import PropTypes from '../../prop-types/index.js';

var EmptyMessage = /*#__PURE__*/React__default.forwardRef(function (props, ref) {
  var addPrefix = props.addPrefix,
      locale = props.locale,
      renderEmpty = props.renderEmpty,
      loading = props.loading;

  if (loading) {
    return null;
  }

  var emptyMessage = /*#__PURE__*/React__default.createElement("div", {
    ref: ref,
    className: addPrefix('body-info')
  }, locale === null || locale === void 0 ? void 0 : locale.emptyMessage);
  return renderEmpty ? renderEmpty(emptyMessage) : emptyMessage;
});
EmptyMessage.displayName = 'Table.EmptyMessage';
EmptyMessage.propTypes = {
  locale: PropTypes.object,
  loading: PropTypes.bool,
  addPrefix: PropTypes.func,
  renderEmpty: PropTypes.func
};

export { EmptyMessage as default };
