'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var intersection = require('../../../lodash/intersection.js');

function shouldShowRowByExpanded(expandedRowKeys, parentKeys) {
  if (expandedRowKeys === void 0) {
    expandedRowKeys = [];
  }

  if (parentKeys === void 0) {
    parentKeys = [];
  }

  var intersectionKeys = intersection['default'](expandedRowKeys, parentKeys);

  if (intersectionKeys.length === parentKeys.length) {
    return true;
  }

  return false;
}

exports['default'] = shouldShowRowByExpanded;
