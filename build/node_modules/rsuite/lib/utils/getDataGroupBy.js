'use strict';

var getDataGroupBy$1 = require('../../../../_virtual/getDataGroupBy.js_commonjs-exports');
require('./treeUtils.js');
var treeUtils = require('../../../../_virtual/treeUtils.js_commonjs-exports');

getDataGroupBy$1.__exports.__esModule = true;
getDataGroupBy$1.__exports.default = getDataGroupBy;
getDataGroupBy$1.__exports.KEY_GROUP_TITLE = getDataGroupBy$1.__exports.KEY_GROUP = void 0;

var _treeUtils = treeUtils.__exports;

var hasSymbol = typeof Symbol === 'function';
var KEY_GROUP = hasSymbol ? Symbol('_$grouped') : '_$grouped';
getDataGroupBy$1.__exports.KEY_GROUP = KEY_GROUP;
var KEY_GROUP_TITLE = 'groupTitle';
getDataGroupBy$1.__exports.KEY_GROUP_TITLE = KEY_GROUP_TITLE;

function getDataGroupBy(data, key, sort) {
  if (data === void 0) {
    data = [];
  }

  var tempData = {};
  var isSort = typeof sort === 'function';
  data.forEach(function (item) {
    if (!tempData[item[key]]) {
      tempData[item[key]] = [];
    }

    tempData[item[key]].push(item);
  });
  var nextData = Object.entries(tempData).map(function (_ref) {
    var _ref2;

    var groupTitle = _ref[0],
        children = _ref[1];
    return _ref2 = {
      children: isSort ? children.sort(sort(false)) : children
    }, _ref2[KEY_GROUP_TITLE] = groupTitle, _ref2[KEY_GROUP] = true, _ref2;
  });

  if (isSort) {
    nextData = nextData.sort(sort(true));
  }

  return (0, _treeUtils.flattenTree)(nextData);
}
