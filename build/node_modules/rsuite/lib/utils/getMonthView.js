'use strict';

var getMonthView = require('../../../../_virtual/getMonthView.js_commonjs-module');
require('../../../@babel/runtime/helpers/interopRequireDefault.js');
var index = require('../../../date-fns/get_day/index.js');
var index$1 = require('../../../date-fns/add_days/index.js');
var interopRequireDefault = require('../../../../_virtual/interopRequireDefault.js_commonjs-module');

(function (module, exports) {

var _interopRequireDefault = interopRequireDefault.interopRequireDefault.exports;

exports.__esModule = true;
exports.default = getMonthView;

var _get_day = _interopRequireDefault(index['default']);

var _add_days = _interopRequireDefault(index$1['default']);

/**
 * Get all weeks of this month
 * @params monthDate
 * @return date[]
 */
function getMonthView(monthDate, isoWeek) {
  var firstDayOfMonth = (0, _get_day.default)(monthDate);
  var distance = 0 - firstDayOfMonth;

  if (isoWeek) {
    distance = 1 - firstDayOfMonth;

    if (firstDayOfMonth === 0) {
      distance = -6;
    }
  }

  var firstWeekendDate = (0, _add_days.default)(monthDate, distance);
  var weeks = [firstWeekendDate];
  var nextWeekendDate = (0, _add_days.default)(firstWeekendDate, 7);
  weeks.push(nextWeekendDate);

  while (weeks.length < 6) {
    nextWeekendDate = (0, _add_days.default)(nextWeekendDate, 7);
    weeks.push(nextWeekendDate);
  }

  return weeks;
}

module.exports = exports.default;
}(getMonthView.getMonthView, getMonthView.getMonthView.exports));
