'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var recompose = require('../../../../_virtual/recompose.js_commonjs-exports');

recompose.__exports.__esModule = true;
recompose.__exports.compose = compose;
recompose.__exports.setStatic = setStatic;
recompose.__exports.setDisplayName = setDisplayName;
recompose.__exports.getDisplayName = getDisplayName;
recompose.__exports.wrapDisplayName = wrapDisplayName;
recompose.__exports.setPropTypes = setPropTypes;

function compose() {
  for (var _len = arguments.length, funcs = new Array(_len), _key = 0; _key < _len; _key++) {
    funcs[_key] = arguments[_key];
  }

  return funcs.reduce(function (a, b) {
    return function () {
      return a(b.apply(void 0, arguments));
    };
  }, function (arg) {
    return arg;
  });
}

function setStatic(key, value) {
  return function (BaseComponent) {
    /* eslint-disable no-param-reassign */
    BaseComponent[key] = value;
    /* eslint-enable no-param-reassign */

    return BaseComponent;
  };
}

function setDisplayName(displayName) {
  return setStatic('displayName', displayName);
}

function getDisplayName(component) {
  if (typeof component === 'string') {
    return component;
  }

  if (!component) {
    return undefined;
  }

  return component.displayName || component.name || 'Component';
}

function wrapDisplayName(component, wrapperName) {
  return wrapperName + "(" + getDisplayName(component) + ")";
}

function setPropTypes(propTypes) {
  return setStatic('propTypes', propTypes);
}

exports.default = recompose.__exports;
