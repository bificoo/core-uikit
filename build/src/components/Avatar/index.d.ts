import { WithChildren, WithStyle } from "../../types/common";
export declare type AvatarProps = WithStyle & WithChildren;
declare const Avatar: (props: AvatarProps) => JSX.Element;
export default Avatar;
