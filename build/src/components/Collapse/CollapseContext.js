import React__default from 'react';

var CollapseContext = React__default.createContext({
    expanded: false,
    setExpanded: function () {
        //
    },
});

export { CollapseContext, CollapseContext as default };
