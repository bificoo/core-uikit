import { ModalPopupProps } from "../ModalPopup";
export declare type CustomModalProps = ModalPopupProps;
declare const CustomModal: (props: CustomModalProps) => JSX.Element;
export default CustomModal;
