import { ReactComponent as SearchIcon } from "../Search/SearchIcon.svg"

const Search = () => {
  return (
    <div style={{ display: "flex" }}>
      <SearchIcon />
    </div>
  )
}

export default Search
